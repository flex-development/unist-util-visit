/**
 * @file color
 * @module unist-util-visit/color
 */

/**
 * Add color to `value`.
 *
 * > **Note**: This function does nothing. It should be used as a stub in
 * > environments where colorized output is not supported.
 *
 * @this {void}
 *
 * @param {string} value - Value to colorize
 * @return {string} Colorized `value`
 */
function color(this: void, value: string): string {
  return value
}

export default color
